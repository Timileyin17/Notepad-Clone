Fix runtime error in the GoTo method where NumberFormatException runs when a certain line is not present in the text
Create 2 methods in the edit class "findNext" and "findPrevious"
Create a "print" function in the File class that can open up the print program.